# Environment Variables Template
# Copy this file to .env.local and fill with real values
# NEVER commit .env or .env.local to git!

# ============================================
# Application
# ============================================

# Environment: development | staging | production
NODE_ENV=development

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Database (Supabase / PostgreSQL)
# ============================================

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Supabase Anon Key (public key, safe for client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (SECRET! Server-side only)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Direct PostgreSQL connection (optional)
DATABASE_URL=postgresql://user:password@host:5432/database

# ============================================
# Authentication
# ============================================

# NextAuth Secret (for JWT tokens)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here

# NextAuth URL
NEXTAUTH_URL=http://localhost:3000

# OAuth Providers (if used)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# ============================================
# API Keys
# ============================================

# OpenAI API (if used)
OPENAI_API_KEY=sk-your_openai_api_key

# Stripe (if used for payments)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# SendGrid / Email Service (if used)
SENDGRID_API_KEY=your_sendgrid_api_key
EMAIL_FROM=noreply@yourdomain.com

# Cloudinary / Image Upload (if used)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ============================================
# Analytics & Monitoring
# ============================================

# Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry Error Tracking
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# Vercel Analytics (automatic on Vercel)
# VERCEL_ANALYTICS_ID=auto

# ============================================
# Feature Flags
# ============================================

# Enable/disable features
NEXT_PUBLIC_FEATURE_NEW_DASHBOARD=false
NEXT_PUBLIC_FEATURE_BETA_ACCESS=false

# ============================================
# Development Only
# ============================================

# Debug mode (development only!)
DEBUG=false

# Show detailed errors (development only!)
NEXT_PUBLIC_SHOW_ERRORS=true

# ============================================
# Security
# ============================================

# CORS allowed domains (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Rate Limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=60

# ============================================
# IMPORTANT: Security
# ============================================

# ❌ NEVER commit this file with real values!
# ✅ Copy to .env.local and fill there
# ✅ .env.local should be in .gitignore
# ✅ Use different keys for dev/staging/production
# ✅ Rotate keys regularly
# ✅ Don't share secrets in Slack/Discord
# ✅ Use secret management (Vercel Env, AWS Secrets Manager, etc)

# ============================================
# How to use:
# ============================================

# 1. Copy file:
#    cp .env.example .env.local

# 2. Fill with real values:
#    - Get Supabase credentials at https://supabase.com
#    - Generate NEXTAUTH_SECRET: openssl rand -base64 32
#    - Get API keys from services you use

# 3. Verify .env.local is in .gitignore:
#    grep ".env.local" .gitignore

# 4. Never commit .env files:
#    git status  # .env.local should not appear

# ============================================
# Variable verification
# ============================================

# In production ensure all critical variables are set:
# - DATABASE_URL or SUPABASE credentials
# - NEXTAUTH_SECRET
# - API keys for used services

# Use runtime validation in src/lib/env.ts:
# if (!process.env.NEXTAUTH_SECRET) {
#   throw new Error('NEXTAUTH_SECRET is not set');
# }
