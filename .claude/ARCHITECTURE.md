---
title: Architecture
type: architecture
status: draft
updated: YYYY-MM-DD
---

# Architecture

<!--
Этот файл — живая карта системы. Не история и не план, а ответ на вопрос
«где что лежит и как связано». Обновляй, когда меняется структура, а не
когда правишь конкретный файл.

Контракт-слой (что должно быть), а не state-слой (что есть прямо сейчас).
State живёт в SNAPSHOT.md, план — в BACKLOG.md, жёсткие запреты — в
INVARIANTS.md.
-->

## Карта системы

<!--
High-level: что вообще есть в проекте. Модули, сервисы, хранилища, внешние
интеграции. Одна строка на единицу. Если нарисуешь ASCII-схему — отлично,
но списком тоже допустимо.
-->

- <!-- Пример: `api/` — HTTP entrypoint, маршрутизация, валидация запросов -->
- <!-- Пример: `core/` — бизнес-логика, изолирована от транспорта и БД -->
- <!-- Пример: `storage/` — адаптеры к SQLite (локально) и Postgres (cloud) -->
- <!-- Пример: внешняя интеграция: Stripe (платежи), SendGrid (email) -->

## Слои и границы

<!--
Кто кому может звонить. Зависимости направлены сверху вниз. Если правило
нарушается — это либо архитектурный долг (записать в BACKLOG), либо ошибка
(записать как нарушение INVARIANT).
-->

- <!-- Пример: `api → core → storage`. Обратные вызовы запрещены. -->
- <!-- Пример: `core` не знает про HTTP, не импортирует ничего из `api/`. -->
- <!-- Пример: тесты могут импортировать что угодно, прод-код — только вниз. -->

## Ключевые контракты

<!--
Имена + одна строка назначения. Детали (схемы полей, коды ошибок, edge
cases) живут в коде или в INVARIANTS.md. Здесь — карта, не справочник.
-->

- <!-- Пример: `POST /api/v1/orders` — создаёт заказ, идемпотентно по `client_token` -->
- <!-- Пример: событие `order.paid` — публикуется после успешной оплаты -->
- <!-- Пример: таблица `users.email` — уникальна, нижний регистр -->

## Точки входа и выходы

<!--
Откуда вообще запускается код и куда он отправляет результат. CLI-команды,
публичные API, cron/scheduled jobs, webhooks.
-->

- <!-- Пример: `npm run dev` — локальный сервер на :3000 -->
- <!-- Пример: cron `cleanup-expired` — раз в сутки в 03:00 UTC -->
- <!-- Пример: webhook `/stripe/events` — входящие события платежей -->

## Зависимости

<!--
Внешние сервисы и библиотеки, которые формируют архитектуру (не каждая
npm-зависимость, а те, что определяют форму системы). Если зависимость
исчезнет — что сломается.
-->

- <!-- Пример: Postgres — основное хранилище, без него ничего не работает -->
- <!-- Пример: Redis — кэш и rate-limit, при падении деградация, не отказ -->
- <!-- Пример: библиотека `zod` — валидация всех внешних входов -->

## Открытые вопросы

<!--
Архитектурные решения, которые ещё не приняты. Это не баги и не задачи —
это развилки, на которых нужно остановиться и подумать прежде, чем
двигаться дальше. Когда вопрос закрыт — переносится в основной текст
выше или удаляется.
-->

- <!-- Пример: единое или раздельное хранилище для аналитики и продакшна? -->
- <!-- Пример: где живёт авторизация — на API-слое или в core? -->
